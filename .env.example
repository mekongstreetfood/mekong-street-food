# Copie ce fichier en .env.local puis remplis les valeurs (Supabase → Project Settings → API)

# Local : "true" = page bientôt. Sur Vercel : ignore cette var — utilise SITE_LIVE.
# La page reste aussi sur /maintenance
MAINTENANCE_MODE=false

# Sur Vercel uniquement : mets "true" quand le resto ouvre (désactive la maintenance)
# SITE_LIVE=true

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
